//Duck Number : a positive number that contains at least one zero,
//but the zero should not be at the beginning.
//5210 yes  0152 no
using System;
namespace ProgramsNumbers
{
    internal class DuckNumber
    {
       /* static void Main(string[] args)
        {
            Console.WriteLine("Enter the number : ");
            string num = Console.ReadLine();
            bool res = false;
            for (int i = 1; i < num.Length; i++)
            {
                if (num[i] == '0')
                {
                    res = true;
                    break;
                }
            }

            if (res)
                Console.WriteLine(num + " is a Duck Number");
            else
                Console.WriteLine(num + " is not a Duck Number");
        }*/
    }
}
