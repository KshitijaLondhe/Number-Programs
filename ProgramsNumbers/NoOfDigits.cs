//No of digits in number
using System;
namespace ProgramsNumbers
{
    internal class NoOfDigits
    {
        /*static void Main(string[] args)
        {
            int num = 123;
            int temp = num;
            string numstring = num.ToString();
            int len=numstring.Length;
            Console.WriteLine(len);
            int count = 0;
            while(num>0)
            {
                int rem = num % 10;
                count++;
                num /= 10;
            }
            Console.WriteLine("No. of digits in "+temp+" are : "+count);
        }*/
    }
}
